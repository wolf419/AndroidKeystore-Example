# Android PQC Keystore Example

A minimal Android app demonstrating how to generate an ML-DSA-65 key pair in Android Keystore, sign data, and verify the signature.

## Requirements

* Android 17 (API 37)
* Hardware Keystore feature level 500+ (KeyMint 5)
* A compatible physical device; standard Android emulators currently report feature level 400

